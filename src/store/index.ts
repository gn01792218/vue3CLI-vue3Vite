import { createStore } from 'vuex'
import auth from './auth'
import table from './table'


export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    auth,table,
  }
})
